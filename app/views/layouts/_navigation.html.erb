<nav class="navbar-default navbar-static-side" role="navigation">
  <div class="sidebar-collapse">
    <ul class="nav metismenu" id="side-menu">
      <li class="nav-header">
        <div class="dropdown profile-element">
          <span class="block m-t-xs font-bold" style="color:white;">
            Log4Me
          </span>
          <% if current_user and current_user.avatar.present? %>
            <%= image_tag current_user.avatar.thumb.url, class: "rounded-circle", size: 50 %>
          <% end %>
          <a data-toggle="dropdown" class="dropdown-toggle" href="#">
            <span class="block m-t-xs font-bold">
              <% if current_user.present? %>
              <%= current_user.name.present? ? current_user.name : current_user.email %><br />
              <span class="m-t-xs" style="font-size:10px;">
                <%= current_user.position if current_user.position.present? %>
              </span>
              <% else %>

              <% end %>
            </span>
            <span class="text-muted text-xs block">Options<b class="caret"></b></span>
          </a>
          <ul class="dropdown-menu animated fadeInRight m-t-xs">
            <% if current_user %>
            <li>
              <%= link_to edit_user_path(current_user), class: "dropdown-item" do %>
              Settings
              <% end %>
            </li>
            <li>
              <%= link_to edit_user_registration_path, class: "dropdown-item" do %>
              Password Change
              <% end %>
            </li>
            <li>
              <%= link_to destroy_user_session_path, method: :delete do %>
              Logout
              <% end %>
            </li>
            <% else %>
            <li>
              <%= link_to new_user_session_path, class: "dropdown-item" do %>
              Sign in
              <% end %>
            </li>
            <% end %>
          </ul>
        </div>
        <div class="logo-element">
          PP+
        </div>
      </li>
      <li class="<%= 'active' if action_name == 'index' and controller_name == 'home' %>">
        <%= link_to "/" do %>
        <i class="fa fa-laptop"></i> <span class="nav-label">Home</span>
        <% end %>
      </li>
      <% if current_user.present? and current_user.access_concepts == true %>
      <li class="<%= 'active' if controller_name == 'concepts' %>">
        <%= link_to concepts_path do %>
        <i class="fa fa-book"></i> <span class="nav-label">Baza Definicji</span>
        <% end %>
      </li>
      <% else %>
      <li class="<%= 'active' if action_name == 'definitions' %>">
        <%= link_to home_definitions_path do %>
        <i class="fa fa-book"></i> <span class="nav-label">Definitions</span>
        <% end %>
      </li>
      <% end %>
      <% if current_user.present? and current_user.access_tags == true %>
      <li class="<%= 'active' if controller_name == 'tags' %>">
        <%= link_to tags_path do %>
        <i class="fa fa-hashtag"></i> <span class="nav-label">Baza Tagów</span>
        <% end %>
      </li>
      <% end %>
      <% if current_user.present? and current_user.access_users == true %>
      <li class="<%= 'active' if controller_name == 'users' %>">
        <%= link_to users_path do %>
        <i class="fa fa-user"></i> <span class="nav-label">Użytkownicy</span>
        <% end %>
      </li>
      <% end %>
      <% if current_user.present? and current_user.access_stats == true %>
      <li class="<%= 'active' if action_name == 'stats' %>">
        <%= link_to home_stats_path do %>
        <i class="fa fa-table"></i> <span class="nav-label">Statystyki</span>
        <% end %>
      </li>
      <% end %>
      <li class="<%= 'active' if action_name == 'contact' %>">
        <%= link_to home_contact_path do %>
          <i class="fa fa-envelope-open-o"></i> <span class="nav-label">Contact Form</span>
        <% end %>
      </li>
      </ul>

    </div>
  </nav>
